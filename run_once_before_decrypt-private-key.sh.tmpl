#!/bin/sh

if [ ! -f "/home/$HOME/.config/chezmoi/key.txt" ]; then
    chezmoi age decrypt --output "/home/$HOME/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/$HOME/.config/chezmoi/key.txt"
fi
